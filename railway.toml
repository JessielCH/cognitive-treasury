[build]
builder = "dockerfile"
buildpacks = ["heroku/python"]

[deploy]
startCommand = "uvicorn api.index:app --host 0.0.0.0 --port $PORT"
restartPolicyMaxRetries = 5

[[services]]
name = "cognitive-treasury-api"
build.context = "."
