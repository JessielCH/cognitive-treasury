[build]
builder = "dockerfile"
buildpacks = ["heroku/python"]

[deploy]
startCommand = "sh -lc \"uvicorn api.index:app --host 0.0.0.0 --port ${PORT:-8000}\""
restartPolicyMaxRetries = 5

[[services]]
name = "cognitive-treasury-api"
build.context = "."
